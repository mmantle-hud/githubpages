---
layout: default
title: Revisiting the My Movie Diary Example
---

# Revisiting the My Movie Diary Example
Previously we created the following model.

**Logical Model for the My Movie Diary Database**

<pre class="mermaid">
erDiagram
    accTitle: Logical model for the My Movie Diary database.
    accDescr: Logical model showing User, Review, Movie and Genre entities.
    users ||--o{ reviews : ""
    users ||--o{ movie_user : ""
    movies ||--o{ movie_user : ""
    reviews }o--|| movies : ""
    movies ||--|{ genre_movie : ""
    genres ||--o{ genre_movie : ""
    users {
        INT user_id PK
        TEXT email_address "NOT NULL"
        TEXT password  "NOT NULL"
    }
    movie_user{
        INT movie_id PK, FK
        INT user_id PK, FK
    }
    movies {
        INT movie_id PK
        TEXT title "NOT NULL"
        INT release_year "NOT NULL"
        INT duration_in_minutes "NOT NULL"
        TEXT primary_language "NOT NULL"
    }
    reviews {
        INT review_id PK
        INT movie_id FK "NOT NULL"
        INT user_id FK "NOT NULL"
        TIMESTAMP review_date "NOT NULL"
        TEXT review_text "NULL"
        INT rating "NOT NULL"
    }
    genre_movie{
        INT genre_id PK, FK
        INT movie_id PK, FK
    }
    genres{
        INT genre_id PK
        TEXT genre_name  "NOT NULL"
        TEXT genre_description  "NOT NULL"
    }
</pre>

Now that we know about lookup tables and associative entities, we can improve the design.

**Updated Logical Model for the My Movie Diary Database**

<pre class="mermaid">
erDiagram
    languages ||--o{ movies : ""
    movies ||--|{ genre_movie : ""
    movies ||--o{ viewings : ""
    movies ||--o{ reviews : ""
    genres ||--o{ genre_movie : ""
    reviews }o--|| users : ""
    users ||--o{ viewings : ""

    users {
        INT user_id PK
        TEXT email_address  "NOT NULL"
        TEXT password  "NOT NULL"
    }
    viewings{
        INT movie_id PK, FK
        INT user_id PK, FK
        TIMESTAMP viewing_date PK   "NOT NULL"
    }
    movies {
        INT movie_id PK
        TEXT title   "NOT NULL"
        INT release_year   "NOT NULL"
        INT duration_in_minutes   "NOT NULL"
        TEXT primary_language FK   "NOT NULL"
    }
    languages{
        TEXT language_code PK
        TEXT language_name   "NOT NULL"
    }
    reviews {
        INT review_id PK
        INT movie_id FK   "NOT NULL"
        INT user_id FK   "NOT NULL"
        TIMESTAMP review_date   "NOT NULL"
        TEXT review_text   "NULL"
        INT rating   "NOT NULL"
    }
    genre_movie{
        INT genre_id PK, FK
        INT movie_id PK, FK
    }
    genres{
        INT genre_id PK
        TEXT genre_name   "NOT NULL"
        TEXT genre_description   "NOT NULL"
    }
</pre>
I have made two key changes.

1. Created a separate `languages` look-up table. This will remove duplicate language names from the `movies` table.
2. If we think about the relationship between User and Movie. We may want to record when the user watched the movie. Therefore, we need to store the time and date of the viewing as part of the relationship between `movie` and `user`. To implement this, I have replaced the `movie_user` junction table with an associative entity, a `viewings` table. 


## Languages doesn't use a surrogate key
Previously we have always used a surrogate primary key e.g. `movie_id`, `city_id`, `student_id`. 

In the `languages` table, we use a three-letter language code as the primary key instead of an ID number. This is a 'natural' key, it stores data that exists in the real-world. 

A language code is used as the primary key because it uniquely identifies each language, remains compact at just three characters, and offers the advantage of being descriptive. When viewed as a foreign key in another table, we can often instantly recognize the language without needing to look up an ID value in a separate table.

Another design choice I have made is naming the foreign key column `primary_language`. Previously we have always named our foreign key columns to match the primary key they reference. In this example, I have chosen `primary_language` as this is more descriptive. Although a movie may feature dialogue in many different languages, this makes it clear it is the main language the movie has been made in. 


**movies**

| movie_id (PK) | title                          | release_year | duration_in_minutes | primary_language (FK) | 
|----------|--------------------------------|--------------|---------------------|------------------| 
| 1        | Casablanca                     | 1943         | 102                 | eng              | 
| 2        | Moonlight                      | 2016         | 111                 | eng              | 
| 3        | Winter's Bone                  | 2010         | 100                 | eng              | 
| 4        | Spirited Away                  | 2001         | 125                 | jpn              | 
| 5        | RRR                            | 2022         | 187                 | tel              | 
| 6        | Crouching Tiger, Hidden Dragon | 2000         | 120                 | zho              |

**languages**

| language_code (PK) | language_name | 
|---------------|---------------| 
| eng           | English       | 
| jpn           | Japanese      | 
| tel           | Telugu        | 
| zho           | Chinese       | 
| dan           | Danish        | 
| hin           | Hindi         | 